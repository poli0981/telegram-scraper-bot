-r requirements.txt

# Test framework
pytest==9.0.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Mocking httpx for dispatcher tests
respx==0.21.1

# Linting (optional but matches Kokone's project conventions)
ruff==0.7.4
